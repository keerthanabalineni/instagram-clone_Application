import {useState} from 'react';import CreatePostModal from '../components/CreatePostModal';export default function Create(){const [story,setStory]=useState(false);return <div className="page create-page"><h1>Create</h1><p>Choose what you want to share with your followers.</p><div className="create-cards"><button onClick={()=>setStory(false)}><strong>＋</strong><b>New post</b><span>Share a photo with a caption.</span></button><button onClick={()=>setStory(true)}><strong>◌</strong><b>New story</b><span>Share a photo that feels like now.</span></button></div><CreatePostModal close={()=>window.history.back()} story={story}/></div>}
import React from 'react';
