import {useState} from 'react';import {useApp} from '../context/AppContext';import PostCard from '../components/PostCard';import CreatePostModal from '../components/CreatePostModal';import {Link} from 'react-router-dom';
import React from 'react';
export default function Profile(){const {profile,posts,users,toggleFollow}=useApp();const [create,setCreate]=useState(false);const mine=posts.filter(p=>p.user===profile.username);return <div className="page"><header className="profile-head"><img className="profile-avatar" src={profile.avatar} alt=""/><div className="profile-info"><div className="profile-title"><h1>{profile.username}</h1><Link className="outline" to="/profile/edit">Edit profile</Link><button className="outline" onClick={()=>setCreate(true)}>＋ Post</button></div><div className="stats"><span><b>{mine.length}</b> posts</span><span><b>1.2K</b> followers</span><span><b>384</b> following</span></div><h3>{profile.name}</h3><p>{profile.bio}</p><a href={`https://${profile.website}`} target="_blank">{profile.website}</a><small>⌖ {profile.location}</small></div></header><div className="tabs"><span className="selected">▦ Posts</span><Link to="/saved">▣ Saved</Link></div>{mine.length?<div className="profile-grid">{mine.map(p=><img key={p.id} src={p.image} alt={p.caption}/>)}</div>:<div className="empty"><h2>Share your first photo</h2><p>When you share a photo, it will appear on your profile.</p><button className="primary small" onClick={()=>setCreate(true)}>Create post</button></div>}{create&&<CreatePostModal close={()=>setCreate(false)}/>}<div className="people-row">{users.slice(0,3).map(u=><button key={u.id} onClick={()=>toggleFollow(u.username)}>{u.following?'Following':'Follow'} @{u.username}</button>)}</div></div>}
