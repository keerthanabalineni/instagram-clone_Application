import {Link} from 'react-router-dom';import {useApp} from '../context/AppContext';export default function Settings(){const {theme,setTheme}=useApp();return <div className="page narrow"><h1>Settings</h1><div className="setting"><div><b>Appearance</b><small>Switch between a comfortable light and dark interface.</small></div><button className="outline" onClick={()=>setTheme(theme==='dark'?'light':'dark')}>{theme==='dark'?'Light':'Dark'} mode</button></div><div className="setting"><div><b>Account</b><small>Your posts, profile and social activity are stored locally in this demo.</small></div><Link className="outline" to="/logout">Log out</Link></div></div>}
import React from 'react';
