import {useState} from 'react';import {useApp} from '../context/AppContext';
import React from 'react';
export default function PostCard({post}){const {profile,toggleLike,toggleSave,addComment,deletePost,updatePost}=useApp();const [comment,setComment]=useState('');const [editing,setEditing]=useState(false);const [caption,setCaption]=useState(post.caption);const mine=post.user===profile.username;const submit=e=>{e.preventDefault();addComment(post.id,comment);setComment('')};return <article className="post-card"><header><img src={post.avatar} alt=""/><div><b>{post.name}</b><small>{post.location} · {post.created}</small></div>{mine&&<div className="post-menu"><button onClick={()=>setEditing(!editing)}>•••</button>{editing&&<div className="menu-pop"><button onClick={()=>{updatePost(post.id,caption);setEditing(false)}}>Save caption</button><button onClick={()=>deletePost(post.id)}>Delete post</button></div>}</div>}</header><img className="post-image" src={post.image} alt={post.caption}/><div className="post-actions"><button onClick={()=>toggleLike(post.id)} className={post.liked?'liked':''}>♥</button><button onClick={()=>document.getElementById(`comment-${post.id}`)?.focus()}>♡</button><button onClick={()=>toggleSave(post.id)} className="save">{post.saved?'🔖':'▢'}</button></div><div className="post-body"><b>{post.likes} likes</b>{editing?<div className="edit-caption"><input value={caption} onChange={e=>setCaption(e.target.value)}/></div>:<p><b>{post.user}</b> {post.caption}</p>}<div className="comments">{post.comments.slice(-2).map((c,i)=><p key={i}><b>{c.user}</b> {c.text}</p>)}</div><form onSubmit={submit} className="comment-form"><input id={`comment-${post.id}`} value={comment} onChange={e=>setComment(e.target.value)} placeholder="Add a comment..."/><button>Post</button></form></div></article>}
